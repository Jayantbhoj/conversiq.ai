{
  "name": "customer-support-rag-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix apps/frontend",
    "dev:backend": "npm run start:dev --prefix apps/backend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix apps/backend",
    "build": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix apps/backend"
  },
  "dependencies": {
    "@google/genai": "^2.6.0",
    "@nestjs/config": "^4.0.4"
  }
}
